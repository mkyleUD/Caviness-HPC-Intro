> ## Explore a Worker Node
> 
> Finally, let's look at the resources available on the worker nodes where your jobs will actually
> run. Try running this command to see the name( %n), CPUs(%c) and memory (%m) available on the worker nodes (the
> instructors will give you the node ID of the compute node to use):
>
> ```
> {{ site.host_prompt }} sinfo -n r04n24 -o "%n %c %m"
> ```
> {: .language-bash}
{: .challenge}
